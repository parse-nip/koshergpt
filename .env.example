# Chat proxy (your Cloudflare Worker URL or any HTTPS endpoint that streams OpenAI-compatible SSE)
VITE_CHAT_API_URL=
# Shared secret your proxy validates on Authorization: Bearer ...
VITE_CHAT_API_KEY=
